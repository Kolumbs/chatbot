# chatbot
Chatbot implementation

## Basic Usage

To use the chatbot, first build wheel:

```bash
python -m build
```

Then install the newly built wheel version:

```bash
pip install dist/chatbot-*-py3-none-any.whl
```

After installation, you can run the chatbot using the following command:

```python
python -m chatbot
```
